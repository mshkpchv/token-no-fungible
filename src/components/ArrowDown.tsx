import * as React from 'react';
import arrowimage from '../assets/arrow-down.svg';

const ArrowImage = () => {
    return (
      <img src={arrowimage} className="arrow-name"/>  
    );
  }
  export default ArrowImage;