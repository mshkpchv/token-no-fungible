import * as React from 'react';
import vaultimage from '../assets/vault.svg';

const VaultImage = () => {
    return (
      <img src={vaultimage} className="vault-image"/>  
    );
  }
  export default VaultImage;